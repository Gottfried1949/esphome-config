# powerpanel.yaml Ersteldatum: 19.02.2026

esphome:
  name: powerpanel_bootloader
  friendly_name: Powerpanel Bootloader

esp32:
  board: esp32-s3-devkitc-1
  framework:
    type: esp-idf

safe_mode:

captive_portal:

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: "Powerpanel Fallback"
    password: "fallback123"

logger:

api:
  encryption:
    key: !secret api_encryption_key
  reboot_timeout: 0s

ota:
  platform: esphome
  password: !secret ota_password
# Feste Fritzbox IP-Adresse: 192.168.178.120  

# Bootlader fertig